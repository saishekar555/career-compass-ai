apiVersion: v1
kind: Service
metadata:
  name: resumeiq-web
  labels:
    app.kubernetes.io/name: resumeiq-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: resumeiq-web
  ports:
    - name: http
      port: 80
      targetPort: http
