trigger:
  branches:
    include:
      - develop
      - main

pool:
  vmImage: ubuntu-latest

variables:
  imageRepository: career-app
  containerRegistry: careeracr12345.azurecr.io
  dockerfilePath: Dockerfile
  tag: $(Build.BuildId)

stages:

- stage: Build
  jobs:
  - job: BuildAndScan
    steps:

    - task: SonarCloudPrepare@3
      inputs:
        SonarCloud: 'SonarCloudConnection'
        organization: 'saishekar555'
        scannerMode: 'CLI'
        configMode: 'manual'
        cliProjectKey: 'saishekar555_career-compass-ai'
        cliProjectName: 'career-compass-ai'
        cliSources: '.'

    - task: SonarCloudAnalyze@3

    - task: SonarCloudPublish@3
      inputs:
        pollingTimeoutSec: '300'

    - task: Docker@2
      displayName: Build and Push Docker Image
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: career-acr-connection
        tags: |
          $(tag)

- stage: Dev
  condition: eq(variables['Build.SourceBranch'], 'refs/heads/develop')
  jobs:
  - deployment: DeployDev
    environment: career-dev
    strategy:
      runOnce:
        deploy:
          steps:
          - task: KubernetesManifest@1
            inputs:
              action: deploy
              kubernetesServiceConnection: career-aks-connection
              manifests: deployment.yaml
              containers: |
                $(containerRegistry)/$(imageRepository):$(tag)

- stage: Prod
  condition: eq(variables['Build.SourceBranch'], 'refs/heads/main')
  jobs:
  - deployment: DeployProd
    environment: career-prod
    strategy:
      runOnce:
        deploy:
          steps:
          - task: KubernetesManifest@1
            inputs:
              action: deploy
              kubernetesServiceConnection: career-aks-connection
              manifests: deployment.yaml
              containers: |
                $(containerRegistry)/$(imageRepository):$(tag)